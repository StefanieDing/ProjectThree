{
  "name": "ProjectThree",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "test": "meteor run --settings settings.json"
  },
  "dependencies": {
    "babel-runtime": "^6.18.0",
    "bcrypt": "^1.0.2",
    "meteor-node-stubs": "~0.2.0"
  }
}
